const video = videoPlayer.querySelector('.video');

function menu() {
    location.href="../index.html";
};

function play_vid() {
    if(video.paused) {
        video.play();
    } else {
        video.pause();
    }
};

function skip() {
    video.s
};

function previous() {

};